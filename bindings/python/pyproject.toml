[project]
name = "ccml-py"
version = "0.1.0"
description = "Thin Python adapter for ccml-ffi"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ccml_py"]
