[package]
name = "ccml-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ccml-core = { path = "../ccml-core" }

[dev-dependencies]
jsonschema = { version = "0.18", default-features = false, features = ["draft202012"] }
serde_json = "1"
